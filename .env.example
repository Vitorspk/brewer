# Brewer Application - Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env files with real credentials!

# ============================================
# Local Development (Docker Compose default)
# ============================================
# These are the default values for local MySQL container
# Docker Compose will use these if no .env file exists

# MySQL Container Configuration
MYSQL_ROOT_PASSWORD=root_password
MYSQL_DATABASE=brewer
MYSQL_USER=brewer
MYSQL_PASSWORD=brewer_password
MYSQL_PORT=3306

# Application Database Connection (local)
DATABASE_URL=jdbc:mysql://mysql:3306/brewer?allowPublicKeyRetrieval=true&useSSL=false
DATABASE_USERNAME=brewer
DATABASE_PASSWORD=brewer_password

# Application Port
APP_PORT=8080

# Spring Profile
SPRING_PROFILES_ACTIVE=dev

# Flyway
FLYWAY_ENABLED=true

# phpMyAdmin Port (optional)
PHPMYADMIN_PORT=8081

# ============================================
# AWS RDS Production (create separate .env.rds)
# ============================================
# To use AWS RDS, create a file named .env.rds with these variables:
# (NEVER commit .env.rds - it's in .gitignore)

# RDS_HOST=your-rds-endpoint.region.rds.amazonaws.com
# RDS_PORT=3306
# RDS_USER=admin
# RDS_PASSWORD=your-secure-password
# RDS_DATABASE=brewer
# RDS_TEST_DATABASE=brewer_test

# Then set these for Docker Compose:
# DATABASE_URL=jdbc:mysql://${RDS_HOST}:${RDS_PORT}/${RDS_DATABASE}?allowPublicKeyRetrieval=true&useSSL=true&requireSSL=false
# DATABASE_USERNAME=${RDS_USER}
# DATABASE_PASSWORD=${RDS_PASSWORD}

# ============================================
# GitHub Actions / CI (configure as Secrets)
# ============================================
# Configure these in GitHub repository:
# Settings > Secrets and variables > Actions

# For production build/deploy:
# DATABASE_URL=jdbc:mysql://...
# DATABASE_USERNAME=admin
# DATABASE_PASSWORD=...

# For integration tests:
# TEST_DB_URL=jdbc:mysql://127.0.0.1:3307/brewer_test?...
# TEST_DB_USER=test
# TEST_DB_PASSWORD=test
# TEST_DB_DRIVER=com.mysql.cj.jdbc.Driver

# ============================================
# AWS Configuration (if using S3, SES, etc.)
# ============================================
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=sa-east-1
# AWS_S3_BUCKET=brewer-photos

# ============================================
# Email Configuration (if using SMTP)
# ============================================
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_FROM=noreply@brewer.com
